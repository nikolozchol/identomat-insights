import { Settings } from 'lucide-react';
import { Placeholder } from '../../components/shell/Placeholder';
export default function SettingsPage() {
  return <Placeholder icon={Settings} title="Settings" blurb="Workspace settings — including the monthly performance report recipients, added later in the build." />;
}
